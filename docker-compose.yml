services:
  api:
    build: .
    container_name: api-vacina-pet
    volumes:
      - ./backend:/app
      - ./logs:/app/logs
      - ./data:/app/db_data
    ports:
      - "8000:8000"
    environment:
      - SECRET_KEY=django-insecure-dev-key-123
      - DEBUG=True
